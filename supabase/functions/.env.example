# Local/sandbox ONLY. Keep all real values outside source control.
BRANKAS_ENV=sandbox
# Disabled unless both this setting and the DB financial_hub_sandbox flag agree.
NUSARTA_EXECUTION_MODE=DISABLED
NUSARTA_SIMULATOR_CALLBACK_SECRET=
# Supabase runtime supplies SUPABASE_URL / ANON_KEY / SERVICE_ROLE_KEY.
